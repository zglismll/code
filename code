链表
最后需要加->next = nullptr;
链表封装  (重组一个链表,Notice:最后需要加->next = nullptr;否则会出现循环)
归并排序常见格式：
if(a==b)
return ;
else
{ int m = (a+b)/2;
   merge(a,m);
   merge(m+1,b);
}

sort(A.begin(),A.end(),cmp);
cmp加static
static int cmp

链表归并排序 
快慢指针；注意区间长度为2个节点的情况
每次剖开加nullptr;

